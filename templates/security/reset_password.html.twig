{% extends 'base.html.twig' %}

{% block title %}Reinitialisation mot de passe{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
                <div class="card my-5">
                    <div class="card-body">
                        <h5 class="card-title text-center">Reinitialisation mot de passe</h5>
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <button class="btn btn-primary btn-block mt-2" type="submit">Envoyer <i class="fas fa-paper-plane"></i></button>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
